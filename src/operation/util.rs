/* fn normalise_name(name: &str) -> String {
    name.chars()
        .filter(char::is_ascii)
        .map(|c| c.to_ascii_lowercase())
        .collect::<String>()
        .trim()
        .to_string()
}
 */
